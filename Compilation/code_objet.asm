TITLE G.asm

PILE SEGMENT STACK
     dw 100 dup(?)
base_pile EQU  this word 
PILE ENDS

ASSUME SS:PILE
MOV AX,PILE
MOV SS, AX
MOV SP, base_pile

DATA SEGMENT
C17 DW ?
C16 DW ?
C15 DW ?
C14 DW ?
C13 DW ?
C11 DW ?
C10 DW ?
TEMP DW ?
C9 DW ?
C6 DW ?
C8 DW ?
J DW ?
B DQ ?
A DQ ?
T DQ 12 dup (?)
D DQ ?
C EQU 2
DATA ENDS

CODE SEGMENT
MAIN:
ETIQ4: MOV AX,C14
ADD AX,2
MOV T1,AX
PUSH AX
ETIQ5: MOV AX,C11
POP BX
CMP AX,BX
JGE ETIQ12
ETIQ6: MOV AX,5
ADD AX,9
MOV T2,AX
PUSH AX
ETIQ7: MOV AX,5
POP BX
ADD AX,BX
MOV T3,AX
PUSH AX
ETIQ9: MOV AX,5
SUB AX,2
MOV T4,AX
PUSH AX
ETIQ11: JMP ETIQ4
ETIQ12: JMP ETIQ15
ETIQ13: MOV AX,B
MOV B,AX
ETIQ14: JMP ETIQ16
ETIQ15: MOV AX,C1]
CMP AX,1
JG ETIQ13
CODE ENDS
END MAIN
